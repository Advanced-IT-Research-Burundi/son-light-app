models:
  Payment:
    order_id: id foreign
    user_id: id foreign
    amount: decimal
    payment_date: date
    payment_method: string
    description: text nullable
    softDeletes: true
    relationships:
      belongsTo: Order, User

controllers:
  Payment:
    resource: web

