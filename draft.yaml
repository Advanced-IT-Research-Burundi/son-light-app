models:
  MaterialUsage:
    stock_id: id foreign
    task_id: id foreign
    user_id: id foreign
    quantity_used: integer
    usage_date: date
    description: text nullable
    softDeletes: true
    relationships:
      belongsTo: Stock, Task, User

controllers:
  MaterialUsage:
    resource: web

 